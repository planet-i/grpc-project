version: v1
plugins:
  - name: go                   # 指定了要使用的插件名称
    out: ../                   # 指定了生成文件的输出目录
    opt: paths=source_relative # 指定了插件的选项
  - name: go-grpc
    out: ../
    opt: paths=source_relative
  - name: grpc-gateway
    out: ../
    opt: paths=source_relative
  - name: openapiv2
    out: ../